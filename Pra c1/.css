body {
    font-family: Arial, sans-serif;
    margin: 0;
    padding: 0;
    background-color: #f2f2f2;
}
header {
    background-color: #dfce38;
    color: #04027e;
    text-align: center;
    padding: 20px;
}
h1 {
    font-size: 36px;
}
p {
    font-size: 18px;
    line-height: 1.6;
    margin: 20px;
    padding: 0 20px;
}
footer {
    background-color: #dfce38;
    color: #04027e;
    text-align: center;
    padding: 10px;
}
html {
    scroll-behavior: smooth;
}

nav ul {
    list-style-type: none;
    padding: 0;
    display: flex; /* Gebruik flexbox */
    justify-content: space-around; /* Verdeel items gelijkmatig over de breedte */
}
nav li {
    margin: 0;
}
nav a {
    text-decoration: none;
}
section {
    padding: 20px;
}
